{
  plugins = {
    # lsp.servers.scheme_langserver = {
    #   enable = true;
    #   settings = {};
    # };
    vim-slime = {
      enable = true;
      settings = {
        # paste_file = "$HOME/.slime_paste";
        # preserve_curpos = 1;
        # default_config = {
        #   socket_name = "default";
        #   target_pane = "{last}";
        # };
        # dont_ask_default = 0;
        # bracketed_paste = 0;
      };
    };
  };
}
